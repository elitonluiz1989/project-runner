<UserControl x:Class="ProjectRunner.WPF.Components.RecordsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectRunner.WPF.Components" xmlns:viewmodels="clr-namespace:ProjectRunner.WPF.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:RecordsManagementViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600"
             d:DesignWidth="400
             ">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles/Components/RecordsList.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ListView
        Grid.Row="1"
        ItemsSource="{Binding Records}"
        Style="{StaticResource RecordsList}">
        <ListView.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            </Style>
        </ListView.ItemContainerStyle>

        <ListView.ItemTemplate>
            <DataTemplate>
                <local:Project DataContext="{Binding }" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
