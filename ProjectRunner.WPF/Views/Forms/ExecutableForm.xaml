<Window x:Class="ProjectRunner.WPF.Views.Forms.ExecutableForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectRunner.WPF.Views.Forms"
        xmlns:components="clr-namespace:ProjectRunner.WPF.Views.Components"
        mc:Ignorable="d"
        Title="Executable"
        Height="150"
        Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Resources/Styles/Views/RecordFormView.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <components:FormField
            Grid.Row="0"
            Label="Name"
            Text=""
            Type="Text" />

        <components:FormField
            Grid.Row="1"
            Label="File"
            Text=""
            Type="File" />
    </Grid>
</Window>
